apiVersion: v1
kind: Pod
metadata:
  name: lifecycle-running

spec:
  restartPolicy: Never
  containers:
    - name: task
      image: busybox:1.36
      command: ["sh", "-c", "echo Hello Kubernetes! && sleep 30"]